/** 自动路由扫描器读取的固定高度演示页元数据。 */
export const page = {
    // 左侧导航和浏览器标题使用的名称。
    title: '固定高度长列表',
    // 页面模板标识；实际入口文件仍由默认 pathFileName=index 决定。
    template: 'fixed-height'
};

// page.ts 约定使用默认导出供 import.meta.glob 读取。
export default page;
