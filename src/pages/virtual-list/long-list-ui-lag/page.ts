/** 长列表优化示例的父级菜单元数据。 */
export const page = {
    // 父级导航菜单显示名称。
    title: '长列表不卡顿',
    // 父级页面模板标识；实际入口文件仍由默认 pathFileName=index 决定。
    template: 'long-list-ui-lag'
};

// 提供给自动路由扫描器读取。
export default page;
