// 对外提供固定高度虚拟列表核心模型。
export { FixedHeightVirtualizer } from './fixed-height-virtualizer';
// 对外提供不定高度虚拟列表核心模型。
export { DynamicHeightVirtualizer } from './dynamic-height-virtualizer';
// 只导出类型，不产生运行时代码。
export type { VirtualRange } from './types';
